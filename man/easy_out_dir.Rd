% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/easy_out.R
\name{easy_out_dir}
\alias{easy_out_dir}
\title{Resolve the output folder of one output}
\usage{
easy_out_dir(
  filename,
  dir = getOption("easy_out.dir", default = "output"),
  subdir = TRUE
)
}
\arguments{
\item{filename}{Output filename, without extension, as passed to
\code{\link[=easy_out]{easy_out()}}. Read only when \code{subdir} is \code{TRUE}.}

\item{dir}{Output directory. Created if it does not exist. Defaults
to \code{getOption("easy_out.dir", "output")}.}

\item{subdir}{Folder created inside \code{dir}. \code{TRUE} (the default) derives it
from \code{filename}, \code{FALSE} gives back \code{dir} itself, and a string names the
folder, taken as given.}
}
\value{
The path of the folder, as an \code{fs_path}.
}
\description{
Give back the directory \code{\link[=easy_out]{easy_out()}} writes into for a given \code{filename},
creating it if it does not exist. Call it from a writer of your own to put
an artefact \code{easy_out()} does not handle, a \code{pptx} among them, beside the
files \code{easy_out()} writes for the same object.
}
\details{
The derived name is \code{filename} lowercased, with every run of
non-alphanumeric characters folded into a single dash and the leading and
trailing ones dropped: \code{fig_surv_strata} gives \code{fig-surv-strata}. The
\code{filename} \code{\link[=easy_out]{easy_out()}} resolves is the expression it was handed rather
than a name, so \code{easy_out(figs$os)} normalises to \code{figs-os}. A \code{filename}
left empty by that cleaning is an error, never a silent fallback on \code{dir}.
Letters outside ASCII count as alphanumeric and are kept as they are
rather than transliterated, accented ones among them.

\code{\link[=easy_out]{easy_out()}} names the files it writes by that same rule, so with no
\code{suffix} the folder and the base of the files it holds are the same
string: a writer of your own reaches that base through
\code{fs::path_file()} of what this function gives back.
}
\examples{
easy_out_dir("fig_surv_strata", dir = fs::path(tempdir(), "output"))

}
\seealso{
\code{\link[=easy_out]{easy_out()}}, which writes into that folder.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes.R
\name{theme_rt}
\alias{theme_rt}
\title{Standardized reactable widget theme}
\usage{
theme_rt(
  alpha = .text_font(),
  font_size = .rt_size,
  bg = "var(--primary-surface, #ffffff)",
  strip_color = str_c("var(--primary-back, ", set_opts(.assign = FALSE)$color$cold[1],
    ")"),
  border_color = "color-mix(in srgb, currentColor, transparent 88\%)",
  expander_color = "var(--bs-body-color, rgba(0, 0, 0, 0.8))",
  search_style = list(width = "100\%"),
  ...
)
}
\arguments{
\item{alpha}{Font family for the widget's text. When \code{\link[=set_opts]{set_opts()}} has been
called, defaults to the centralised text font (\code{opts$font$alpha});
otherwise the OS-agnostic system sans-serif (\code{"sans"}).}

\item{font_size}{CSS font size for the widget's text. \code{\link[=get_vars_dict]{get_vars_dict()}}
counts its column widths in this size, so a theme carrying another one
resizes them too.}

\item{bg}{Background color of the widget. \code{reactable} paints \code{.Reactable}
white in its own stylesheet, so a widget left without this renders as a
white card carrying light text under a dark scheme.}

\item{strip_color}{Background color of the striped rows. The fallback is the
first cold-palette color from the options.}

\item{border_color}{Color of the cell rules. A translucent mix rather than a
token, so it composes on whichever surface carries it and follows the
scheme on its own.}

\item{expander_color}{Color of the group expander arrow, which \code{reactable}
paints at \code{rgba(0, 0, 0, 0.8)} and no \code{\link[reactable:reactableTheme]{reactable::reactableTheme()}}
argument reaches: it goes in through a nested selector key instead. Never
\code{currentColor}, which resolves to \code{transparent} here, \code{reactable} setting
\code{color: transparent} on the element carrying the arrow.}

\item{search_style}{Style of the search input, forwarded to
\code{\link[reactable:reactableTheme]{reactable::reactableTheme()}}.}

\item{...}{Additional arguments forwarded to \code{\link[reactable:reactableTheme]{reactable::reactableTheme()}}.}
}
\value{
A \code{\link[reactable:reactableTheme]{reactable::reactableTheme()}} object.
}
\description{
Builds the package's house palette for a \code{\link[reactable:reactable]{reactable::reactable()}} widget, the
HTML-only member of the table themes. Unlike \code{\link[=theme_gt]{theme_gt()}} and \code{\link[=theme_ft]{theme_ft()}},
which take a table and return a table, this one takes no table and returns a
value: \code{reactable} reads its theme at construction and offers no way to
restyle a widget afterwards. Pass it at the call site,
\code{reactable(theme = theme_rt())}, or once for a session,
\code{options(reactable.theme = theme_rt())}, which \code{\link[reactable:reactable]{reactable::reactable()}}
reads as its own default.
}
\details{
The color defaults are CSS custom properties carrying a literal fallback,
which \code{reactable} serialises to the DOM as written. Under a document
declaring \code{--primary-surface} and \code{--primary-back} (the \code{hebstr-doc} Quarto
theme does) the widget follows the light and dark schemes; anywhere else,
the standalone artifacts \code{\link[=easy_out]{easy_out()}} writes included, every token falls
back to its literal and the rendering is the package palette. Ground and
striping are the inverse of \code{\link[=theme_gt]{theme_gt()}}, which tints its body and leaves
its striping white: a \code{reactable} follows the ordinary convention of a white
ground and a tinted stripe.
}
\examples{
theme_rt()

theme_rt(strip_color = "#F5FBFF")

}

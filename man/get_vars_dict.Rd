% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_vars_dict.R
\name{get_vars_dict}
\alias{get_vars_dict}
\title{Variable dictionary of a data frame}
\usage{
get_vars_dict(x, cols = everything(), theme = theme_rt(), ...)
}
\arguments{
\item{x}{A data frame to summarise.}

\item{cols}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Columns of the
summary to show in the widget, selected on their displayed names (\code{n},
\code{variable}, \code{label}, \code{type}, \code{n_miss}, \code{p_miss}, \code{range}, \code{q1_med_q3},
\code{levels}) rather than on the \code{pos} the returned \code{data} carries. Defaults to
every column of the summary; a column the frame does not carry (\code{range} on
a frame with no numeric column, say) is absent from the selection too.}

\item{theme}{A \code{\link[reactable:reactableTheme]{reactable::reactableTheme()}} object styling the widget,
defaulting to the package theme \code{\link[=theme_rt]{theme_rt()}}. The default is that value
rather than a read of \code{getOption("reactable.theme")}, so the widget does
not depend on whether a project set that option before or after this call.
The column widths are counted in the theme's font size, so a theme
carrying another one resizes them too.}

\item{...}{Additional arguments forwarded to \code{\link[reactable:reactable]{reactable::reactable()}}.}
}
\value{
A \code{hebstr_dict} object: a named list with \code{data} (the per-variable
summary tibble), \code{json} (the JSON-friendly copy) and \code{output} (the
interactive \code{reactable} widget). \code{data} flattens the multi-valued cells
(\code{range}, \code{q1_med_q3}, \code{levels}) into \code{" ; "}-separated strings, where
\code{json} keeps them as vectors marked with \code{\link[base:I]{base::I()}} so that a JSON writer
emits arrays rather than strings, counts missing values as \code{0} instead of
\code{NA} and carries \code{p_miss} as a proportion rather than a formatted
percentage. The class is what \code{\link[=easy_out]{easy_out()}} dispatches on to write the
widget and the summary together, as HTML, XLSX and JSON.
}
\description{
Summarises each variable of a data frame (type, missingness, range, quartiles,
factor levels) and returns that dictionary three ways: as a tibble, as an
interactive \code{\link[reactable:reactable]{reactable::reactable()}} widget, and as a JSON-friendly copy.
}
\examples{
\dontrun{
get_vars_dict(datasets::penguins)
get_vars_dict(datasets::penguins, cols = c(n, variable, type, n_miss))
}

}

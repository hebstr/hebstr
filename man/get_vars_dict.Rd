% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_vars_dict.R
\name{get_vars_dict}
\alias{get_vars_dict}
\title{Variable dictionary of a data frame}
\usage{
get_vars_dict(
  x,
  cols = everything(),
  font_size = "0.7rem",
  font_family = .text_font(),
  strip_color = set_opts(.assign = FALSE)$color$cold[1],
  json = FALSE,
  ...
)
}
\arguments{
\item{x}{A data frame to summarise.}

\item{cols}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Columns of the
summary to show in the widget, selected on their displayed names (\code{n},
\code{variable}, \code{label}, \code{type}, \code{n_miss}, \code{p_miss}, \code{range}, \code{q1_med_q3},
\code{levels}) rather than on the \code{pos} the returned \code{data} carries. Defaults to
every column of the summary; a column the frame does not carry (\code{range} on
a frame with no numeric column, say) is absent from the selection too.}

\item{font_size}{CSS font size for the widget's text.}

\item{font_family}{Font family for the widget's text. When \code{\link[=set_opts]{set_opts()}} has
been called, defaults to the centralised text font (\code{opts$font$alpha});
otherwise the OS-agnostic system sans-serif (\code{"sans"}).}

\item{strip_color}{Background color of the striped rows. Defaults to the first
cold-palette color from the options.}

\item{json}{Also return a JSON-friendly copy of the summary? It keeps the
multi-valued cells as list columns marked with \code{\link[base:I]{base::I()}}, so that a JSON
writer emits arrays rather than strings, counts missing values as \code{0}
instead of \code{NA} and carries \code{p_miss} as a proportion rather than a
formatted percentage.}

\item{...}{Additional arguments forwarded to \code{\link[reactable:reactable]{reactable::reactable()}}.}
}
\value{
A named list with \code{data} (the per-variable summary tibble), \code{output}
(the interactive \code{reactable} widget) and \code{json} (the JSON-friendly copy, or
\code{NULL}). \code{data} flattens the multi-valued cells (\code{range}, \code{q1_med_q3},
\code{levels}) into \code{" ; "}-separated strings.
}
\description{
Summarises each variable of a data frame (type, missingness, range, quartiles,
factor levels) and returns that dictionary three ways: as a tibble, as an
interactive \code{\link[reactable:reactable]{reactable::reactable()}} widget, and, on demand, as a
JSON-friendly copy.
}
\examples{
\dontrun{
get_vars_dict(datasets::penguins)
get_vars_dict(datasets::penguins, cols = c(n, variable, type, n_miss))
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col_missing.R
\name{col_missing}
\alias{col_missing}
\title{Collapse the missing-value rows into a single column}
\usage{
col_missing(
  x,
  prefix = "",
  empty = "0",
  header = str_glue("**{check_opts(labs$col_missing)}**"),
  align = "center"
)
}
\arguments{
\item{x}{A \code{gtsummary} table, typically built with
\code{\link[gtsummary:tbl_summary]{gtsummary::tbl_summary()}}.}

\item{prefix}{Character string prepended to each collapsed count (e.g.
\code{"n ="}), separated from it by a space. Defaults to \code{""}, which leaves the
count on its own.}

\item{empty}{Replacement for a missing per-group count. Defaults to \code{"0"}.}

\item{header}{Column header for the \code{dm} column, interpreted as markdown.
Defaults to the \code{labs$col_missing} option wrapped in bold (\code{MD} in English,
\code{DM} in French).}

\item{align}{Column alignment for the \code{dm} column. Defaults to \code{"center"}.}
}
\value{
The \code{gtsummary} table with the missing rows collapsed into a \code{dm}
column, or the input unchanged (with a warning) when a \code{dm} column already
exists or no missing rows are present.
}
\description{
Extracts the automatic \code{gtsummary} "missing" rows (one per variable) and
folds their per-group counts into a single \code{dm} column placed on the label
row, then drops the original missing rows. The counts are joined with \code{/}
across the grouping columns (or the overall column when the table is
ungrouped).
}
\details{
The function is idempotent: a table that already carries a \code{dm} column is
returned unchanged with a warning, so the manual call and the
\code{\link[=tbl_format]{tbl_format()}} \code{missing} switch never stack. A table with no missing rows is
likewise returned unchanged with a warning.
}
\examples{
set_opts()

df <- data.frame(
  grp = c(rep("a", 10), rep("b", 10)),
  age = c(seq_len(18), NA, NA)
)
tbl <- gtsummary::tbl_summary(df, by = grp, include = age)
col_missing(tbl)

}

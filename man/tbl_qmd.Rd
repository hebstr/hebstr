% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qmd_helpers.R
\name{tbl_qmd}
\alias{tbl_qmd}
\title{Create a styled table for Quarto documents}
\usage{
tbl_qmd(
  data,
  top_n = NULL,
  font_family = .text_font(),
  font_size = 15,
  id = NULL,
  width = 700,
  page_width = check_opts(page_width),
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{gtsummary} object.}

\item{top_n}{If not \code{NULL}, a positive integer passed to \code{\link[gt:gt_preview]{gt::gt_preview()}}
to display only the first \code{top_n} rows, the last one, and an ellipsis row
between them. Ignored on a \code{gtsummary} input, which is converted whole.
The Word branch renders the very rows \code{\link[gt:gt_preview]{gt::gt_preview()}} selects.}

\item{font_family}{Font family for the table's text. When \code{\link[=set_opts]{set_opts()}} has
been called, defaults to the centralised text font (\code{opts$font$alpha});
otherwise the OS-agnostic system sans-serif (\code{"sans"}).}

\item{font_size}{Font size in pixels. Defaults to \code{15}. Converted to points
(\code{font_size * 0.75}) on the Word branch, whose native unit is the point.}

\item{id}{HTML id attribute for the table. Defaults to \code{NULL}, which lets
\code{\link[gt:gt]{gt::gt()}} generate a random unique id. This id scopes gt's own stylesheet,
so a fixed default makes every table in a document share one scope, where
the last stylesheet wins for all of them. A readable, stable anchor belongs
on the Quarto crossref label, not here. Has no effect on the \code{top_n}
preview of a data frame: \code{\link[gt:gt_preview]{gt::gt_preview()}} builds the table itself and
exposes no id argument, nor on the Word branch, which writes no HTML.}

\item{width}{Table width, in pixels. Defaults to \code{700}. On the Word branch it
is converted to the fraction of \code{page_width} that
\code{\link[flextable:set_table_properties]{flextable::set_table_properties()}} expects, capped at \code{1}. Set to \code{NULL} to
let the table keep its natural width.}

\item{page_width}{Usable text width, in inches, used to convert \code{width} on
the Word branch, and read on that branch only. Defaults to the \code{page_width}
option, resolved as in \code{\link[=tbl_format]{tbl_format()}}.}

\item{...}{Additional arguments passed to \code{\link[gt:tab_options]{gt::tab_options()}}, or to
\code{\link[=theme_ft]{theme_ft()}} under \code{options(hebstr.docx = TRUE)}. Set the table width
through \code{width} rather than here: the two branches take it in different
units.}
}
\value{
A \link[gt:gt]{gt::gt} object, or a \code{flextable} object under
\code{options(hebstr.docx = TRUE)}. Style verbs applied downstream must handle
both classes: see \code{\link[=tbl_font_size]{tbl_font_size()}} and \code{\link[=tbl_row_color]{tbl_row_color()}}.
}
\description{
Converts a data frame or gtsummary object into a themed table with custom
font and bold column labels: a \link[gt:gt]{gt::gt} table, or a
\code{\link[flextable:flextable]{flextable::flextable()}} styled by \code{\link[=theme_ft]{theme_ft()}} under
\code{options(hebstr.docx = TRUE)}. The data frame twin of \code{\link[=tbl_format]{tbl_format()}}, which
finalises a \code{gtsummary} table through the same two branches.
}
\details{
\code{gt} writes no width information into its Word output: neither \code{w:tblW}, nor
\code{w:tblLayout}, nor \code{w:tblGrid}, so Word collapses every column to its
minimum content width and breaks inside words. The Word branch therefore
renders through \code{flextable}, for the same reason as \code{\link[=tbl_format]{tbl_format()}}'s.

That branch styles through \code{\link[=theme_ft]{theme_ft()}}, which reads the centralised
options: it requires \code{\link[=set_opts]{set_opts()}}, where the \code{gt} branch does not.
}
\examples{
tbl_qmd(mtcars)
tbl_qmd(mtcars, top_n = 2)

}

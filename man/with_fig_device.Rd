% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/easy_out.R
\name{with_fig_device}
\alias{with_fig_device}
\title{Build a grid graphic against the device that will draw it}
\usage{
with_fig_device(width, height, code)
}
\arguments{
\item{width, height}{Device size in inches. Pass the values \code{\link[=easy_out]{easy_out()}}
will be called with.}

\item{code}{Expression building the graphic. Evaluated once, on the device
this function opens, so it has to be the construction itself rather than
a graphic built beforehand. A bare symbol is rejected: its value was
computed before the call, which leaves nothing for the device to measure.}
}
\value{
The value of \code{code}.
}
\description{
Opens an off-screen device of the given size, evaluates \code{code} on it, then
closes it and restores the device that was current before. Pass the size
the graphic will be exported at, so that a layout computed while the grob
is built matches the canvas it ends up on.
}
\details{
\code{grid} resolves a unit against the device current at the moment of the
conversion, and a package that precomputes layout, \code{Gmisc} among them,
performs that conversion when the grob is created rather than when it is
drawn. A \code{Gmisc::boxGrob()} built in the IDE plot pane therefore carries
coordinates measured against the pane, and drawing it on the canvas
\code{\link[=easy_out]{easy_out()}} opens displaces everything derived from a box edge:
\code{Gmisc::connectGrob(type = "N")} puts its horizontal segment halfway
between two boxes, and that half-distance shrinks as the construction
device gets shorter.

The symptom is one script whose figure differs between the plot pane, a
'Quarto' render and the exported file. Nothing errors, and the boxes
themselves look right, their text being measured in absolute units.

Hoisting the construction into a variable reinstates that symptom, since
\code{code} is then a symbol whose value the enclosing assignment has already
forced. Splitting the construction out stays possible through a function,
called from \code{code}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{.flow_grob <- \\() \{ ... \}

with_fig_device(width, height, code = .flow_grob())
}\if{html}{\out{</div>}}
}
\section{Device family}{

Text metrics differ from one device family to the next, so the construction
device has to come from the same family as the export. Measured on a
five-line box at 11 pt: \code{svglite} 156.43 pt wide, \code{cairo_pdf} 157.60 pt,
\code{ragg} 156.38 pt, and \code{pdf(NULL)} 150.08 pt, the last one reaching no
system font at all. \code{\link[withr:with_svg]{withr::with_svg()}} wraps the cairo device and so does
not qualify here.

\code{svglite::svgstring()} renders to a memory buffer rather than a file. It
measures identically to \code{svglite::svglite()} and writes nothing to disk.
}

\section{Cleanup}{

The device is closed by its number, captured on opening. Closing whichever
device happens to be current on exit would close the wrong one as soon as
\code{code} leaves a device behind, which any error raised after a graphics call
does.
}

\examples{
\dontrun{
size <- list(width = 11.5, height = 8)

flowchart <- with_fig_device(size$width, size$height, {
  screened <- Gmisc::boxGrob("Screened (n = 333)", x = 0.4, y = 0.7)
  included <- Gmisc::boxGrob("Included (n = 103)", x = 0.4, y = 0.3)

  grid::grobTree(
    screened,
    included,
    Gmisc::connectGrob(screened, included, type = "N")
  )
})

easy_out(flowchart, width = size$width, height = size$height)
}

}
\seealso{
\code{\link[=easy_out]{easy_out()}}, which draws the graphic on a device of that size.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/easy_out.R
\name{easy_out}
\alias{easy_out}
\title{Save a ggplot, gt table, grid graphic, widget, or workbook to disk}
\usage{
easy_out(
  x,
  filename = NULL,
  dir = getOption("easy_out.dir", default = "output"),
  subdir = TRUE,
  suffix = "",
  sep = "_",
  width = NULL,
  height = NULL,
  px = 1200,
  crop = getOption("easy_out.crop", default = TRUE),
  pptx = getOption("easy_out.pptx", default = FALSE),
  quiet = getOption("easy_out.quiet", default = FALSE),
  export = getOption("easy_out.export", default = !.is_docx()),
  web_fonts = .web_fonts()
)
}
\arguments{
\item{x}{A ggplot, ggmatrix, gt_tbl, gtsummary, grid grob (for example a
Gmisc flowchart built from \code{boxGrob()}/\code{connectGrob()}), htmlwidget (a
\code{\link[reactable:reactable]{reactable::reactable()}}, say), \code{hebstr_dict} (what \code{\link[=get_vars_dict]{get_vars_dict()}}
returns), or wbWorkbook object, such as the one \code{\link[=get_xlsx]{get_xlsx()}} returns.}

\item{filename}{Output filename (without extension). Defaults to the name
of the object passed as \code{x}, and is required when \code{x} is anything else
than a name: the deparsed call would name the folder and the file after
the whole expression. Written out in kebab-case: lowercased, with every
run of non-alphanumeric characters folded into a single dash, so
\code{tbl_demo} writes \code{tbl-demo.html}.}

\item{dir}{Output directory. Created if it does not exist. Defaults
to \code{getOption("easy_out.dir", "output")}.}

\item{subdir}{Folder created inside \code{dir} to hold this output. \code{TRUE} (the
default) derives it from \code{filename} by that same rule, so \code{fig_surv_strata}
writes \code{fig-surv-strata/fig-surv-strata.svg}. The derivation happens
before \code{suffix} is appended, so the variants of one output share a
folder. \code{FALSE} writes straight into \code{dir}. A string names the folder
itself, taken as given.}

\item{suffix}{Optional suffix appended to \code{filename}.}

\item{sep}{Separator between \code{filename} and \code{suffix}. Folded into a dash
along with the rest of the name, so it separates without surviving
verbatim.}

\item{width}{Width of the output. For tables: table width in pixels,
overriding the width the object carries from \code{\link[=tbl_format]{tbl_format()}}. When left
\code{NULL}, a table declaring its own width keeps it, and one declaring none
gets 700. For plots and grid graphics: SVG width in inches
(default 7). For a grid grob under \code{crop = TRUE}, \code{width} and \code{height}
are a canvas budget rather than the size of the exported file, which
is trimmed back to the drawing it contains. Ignored for widgets, which
carry their own layout.}

\item{height}{Height in inches for SVG output of plots and grid graphics
only. \code{NULL} (default) uses the nombre d'or: \code{width / 1.618}. Ignored
for tables, widgets and workbooks.}

\item{px}{Height in pixels for the PNG rasterization of plots and grid
graphics.}

\item{crop}{If \code{TRUE} (the default, read from
\code{getOption("easy_out.crop")}), trim the SVG canvas of a grid grob to
the bounding box of the drawing, keeping a small margin. Grob positions
are relative to the whole page, so a drawing covering a sub-rectangle
leaves an empty band that no \code{width}/\code{height} value removes. Ignored
for tables and plots, whose margins come from the theme, and for widgets
and workbooks. Ignored for the PPTX slide too, which carries the whole
canvas: an editable object is cropped in the tool that opens it.}

\item{pptx}{If \code{TRUE}, also write the plot or grid graphic to a PPTX slide,
as an editable DrawingML shape rather than an image. Defaults to
\code{getOption("easy_out.pptx", FALSE)}, and requires the \pkg{rvg} package.
The slide is the default 4:3 \verb{Office Theme} one, and the drawing is
scaled to fit it and centred. Fonts are named in the slide rather than
embedded, so a reader without the family installed gets a substitute.
Tables, widgets and workbooks have no slide form and error out.}

\item{quiet}{If \code{TRUE}, suppress auto-opening the output in a browser. Defaults
to \code{getOption("easy_out.quiet", FALSE)}.}

\item{export}{If \code{FALSE}, return without writing anything. Defaults to
\code{getOption("easy_out.export")}, itself defaulting to \code{FALSE} under
\code{options(hebstr.docx = TRUE)}: a Word run renders tables through
\code{\link[flextable:flextable]{flextable::flextable()}}, leaving no object to export to HTML or PNG.
The guard is read from the session option alone, so a workbook exported
during a Word run needs an explicit \code{export = TRUE}.}

\item{web_fonts}{Faces embedded into the SVG, as \code{\link[svglite:font_face]{svglite::font_face()}}
blocks. Defaults to the bundled faces for the font \code{set_opts()} resolved,
and to \code{NULL} for a family the package does not ship, which leaves the SVG
rendering in whatever the reader has installed. Supply your own to cover
another family; build them with \code{woff2} given as a \verb{data:} URI rather than
\code{embed = TRUE}, which re-emits the face as uncompressed TTF.}
}
\value{
\code{NULL} (invisibly). Called for its side effects.
}
\description{
Export a ggplot, gt, gtsummary, or grid grob object to PNG (and SVG or
HTML depending on the object type), an htmlwidget to HTML, or an \code{openxlsx2}
workbook to XLSX. A figure also goes to an editable PPTX slide under
\code{pptx = TRUE}, and the variable dictionary \code{\link[=get_vars_dict]{get_vars_dict()}} returns also
goes to XLSX and JSON. Opens the result in a browser unless \code{quiet = TRUE}.
}
\details{
In a remote session, the exported file lives on the server while the
browser runs on the local machine, so a file path cannot be opened.
\code{easy_out()} then serves \code{dir} over HTTP on the loopback interface and
opens \verb{http://localhost:<port>/<file>}, which the IDE forwards.
The choice is read from \code{getOption("easy_out.serve")}: \code{NULL} (the
default) detects a remote session through \code{SSH_CONNECTION}, while \code{TRUE}
or \code{FALSE} forces the HTTP or the file-path route. Its port comes from
\code{getOption("easy_out.port")}, a free one when unset. The server is started
once per session, and restarted whenever \code{dir} or the requested port
changes. Should it fail to start, the file path is opened instead.
\code{subdir} never restarts it: the root served stays \code{dir}, and the folder
comes through as a path segment of the URL.
}
\examples{
\dontrun{
easy_out(my_plot)
easy_out(my_table, suffix = "v2", quiet = TRUE)
easy_out(get_xlsx(list(iris = iris)), filename = "tables")
}

}

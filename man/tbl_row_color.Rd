% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_style.R
\name{tbl_row_color}
\alias{tbl_row_color}
\title{Set the text color of table rows}
\usage{
tbl_row_color(x, rows, color)
}
\arguments{
\item{x}{A \code{gt_tbl} or a \code{flextable} object, typically from \code{\link[=tbl_format]{tbl_format()}}.}

\item{rows}{A predicate expression evaluated against the table body, used to
select the rows to style.}

\item{color}{Text color, as a hex string or a color name.}
}
\value{
An object of the same class as \code{x}.
}
\description{
Applies a text color to the body rows matching a predicate, on either a
\code{\link[gt:gt]{gt::gt()}} or a \code{\link[flextable:flextable]{flextable::flextable()}} object. Companion to
\code{\link[=tbl_font_size]{tbl_font_size()}}; see there for the dual-branch rationale.
}
\details{
The predicate is evaluated against the table body. On the \code{flextable} branch
that body holds the visible columns only, so a predicate targeting a column
hidden by \code{tbl_format()} (such as \code{variable} or \code{row_type}) resolves on the
\code{gt_tbl} branch and fails on the \code{flextable} one.
}
\examples{
tbl <- gt::gt(head(penguins)) |>
  tbl_row_color(rows = species == "Adelie", color = "#B22222")

}

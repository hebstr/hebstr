% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opts.R
\name{lang_fr}
\alias{lang_fr}
\title{Set French locale for R and gtsummary}
\usage{
lang_fr(reset = FALSE)
}
\arguments{
\item{reset}{Logical. If \code{FALSE} (default), activates French locale.
If \code{TRUE}, restores English defaults.}
}
\value{
Called for its side effects (invisible \code{NULL}). Modifies
\code{options(OutDec)} and the gtsummary theme.
}
\description{
Configures the R environment to use French formatting conventions,
including the comma as decimal separator and French language settings
for gtsummary tables. Also allows resetting to English defaults.
}
\details{
Call it before \code{\link[=set_opts]{set_opts()}}: the options object takes its labels and
decimal mark from the locale in force when it is built, so switching
language afterwards leaves it in the previous one. A warning says so when
the language changes while an options object already exists.
}
\examples{
withr::with_options(list(OutDec = "."), {
  lang_fr()
  getOption("OutDec")

  lang_fr(reset = TRUE)
  getOption("OutDec")
})

}
\seealso{
\code{\link[gtsummary:theme_gtsummary_language]{gtsummary::theme_gtsummary_language()}}

Other configuration:
\code{\link[=check_opts]{check_opts()}},
\code{\link[=clear_vars]{clear_vars()}},
\code{\link[=docx_page_width]{docx_page_width()}},
\code{\link[=get_opts]{get_opts()}},
\code{\link[=set_opts]{set_opts()}},
\code{\link[=use_vars]{use_vars()}}
}
\concept{configuration}

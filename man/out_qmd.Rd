% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qmd_helpers.R
\name{out_qmd}
\alias{out_qmd}
\title{Publish an easy_out() output in a rendered document}
\usage{
out_qmd(
  x,
  filename = NULL,
  dir = getOption("easy_out.dir", default = "output"),
  subdir = TRUE,
  suffix = "",
  sep = "_"
)
}
\arguments{
\item{x}{A supported object, or one element of a named list of them, as
\code{x$element}. Passed by name: the path is derived from it.}

\item{filename}{Base name the output was written under, when \code{\link[=easy_out]{easy_out()}}
was given one. Defaults to \code{NULL}, which derives it from \code{x}.}

\item{dir, subdir, suffix, sep}{The path-forming arguments of \code{\link[=easy_out]{easy_out()}},
with the same defaults and the same meaning. Pass here whatever the
writing call passed.}
}
\value{
The object itself, or the artefact wrapped by
\code{\link[knitr:include_graphics]{knitr::include_graphics()}}.
}
\description{
Hands the document either the live object or the artefact \code{\link[=easy_out]{easy_out()}}
wrote for it, whichever the render target can carry. The object is passed
by name, and the path of its artefact is derived with the rules
\code{\link[=easy_out]{easy_out()}} used to write it, so a document never restates a filename and
the two sides cannot drift apart.
}
\details{
What is handed over follows the object's class and the render target:\tabular{lll}{
   Class \tab HTML \tab Any other target \cr
   \code{gt_tbl}, \code{gtsummary} \tab the object \tab the PNG \cr
   \code{flextable} \tab the object \tab the object \cr
   \code{htmlwidget} \tab the object \tab the PNG \cr
   \code{hebstr_dict} \tab the widget \tab the PNG \cr
   ggplot, \code{ggmatrix}, grid grob \tab the SVG \tab the PNG \cr
}


The class carries the format decision the document made upstream: under
\code{options(hebstr.docx = TRUE)} a table is a \code{flextable} and goes to Word
whole, whereas a document leaving the option unset gets a \code{gt_tbl} and its
PNG. One reader serves both.

A figure is always the file its script wrote, never a redraw. Left to
knitr, it would be drawn on the chunk device, at other dimensions and with
other fonts, and no error would say so.

A bare list is a multi-panel figure and nothing else, published as one
figure holding every panel. Reading a table or a widget out of a list is
refused rather than allowed: it would work under Word, where every panel is
a path, and break under HTML, where a list of live objects prints as a
list.

A widget has a PNG only when \code{\link[=easy_out]{easy_out()}} was called with \code{png = TRUE}; the
missing file aborts the render rather than dropping the output.
}
\examples{
\dontrun{
out_qmd(tbl_pop)
out_qmd(tbl_codes$cim10)
out_qmd(fig_paths)
}

}

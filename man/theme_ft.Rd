% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes.R
\name{theme_ft}
\alias{theme_ft}
\title{Standardized flextable theme}
\usage{
theme_ft(
  x,
  width = NULL,
  alpha = .font_fallback[[1]],
  digit = alpha,
  base = "#333333",
  color = check_opts(color$cold[1]),
  bg = "white",
  row_padding = 3,
  title_align = "left",
  title_font_size = .ft_title$size,
  font_size = .ft_size,
  stat_font_size = font_size - 1,
  pvalue_font_size = font_size - 2,
  row_strip = TRUE,
  footnote_font_size = font_size - 2,
  footnote_padding = row_padding,
  repeat_header = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{\link[flextable:flextable]{flextable::flextable()}} table object to style.}

\item{width}{Table width, as a fraction of the available page width, from 0
to 1. When \code{NULL}, the table keeps its natural width.}

\item{alpha}{Font family for the table text. Defaults to \code{"Aptos"}, whatever
the session font.}

\item{digit}{Font family applied to numeric columns (stats, estimates,
p-values). Defaults to \code{alpha}.}

\item{base}{Base color for text and body borders.}

\item{color}{Table background color, also used for the row-striping band.}

\item{bg}{Background color for the column labels, striping, and footnotes.}

\item{row_padding}{Vertical padding of data rows, in points.}

\item{title_align}{Horizontal alignment of the caption (\code{"left"},
\code{"center"}, \code{"right"}, or \code{"justify"}).}

\item{title_font_size}{Caption font size, in points.}

\item{font_size}{Base font size, in points.}

\item{stat_font_size}{Font size of stat and estimate cells, in points.}

\item{pvalue_font_size}{Font size of p-value cells, in points.}

\item{row_strip}{Whether to color the striped rows. When \code{FALSE}, the
striping band is made transparent.}

\item{footnote_font_size}{Footnote font size, in points.}

\item{footnote_padding}{Footnote padding, in points.}

\item{repeat_header}{Whether Word repeats the header rows at the top of each
page a split table runs onto. Defaults to \code{FALSE}, so the continuation
opens on its data rows. A \code{repeat_headers} entry of \code{opts_word} passed
through \code{...} takes precedence. The option is set by the same call to
\code{\link[flextable:set_table_properties]{flextable::set_table_properties()}} as \code{width}: a later call to that
function resets it to the \code{flextable} default, which repeats.}

\item{...}{Additional properties forwarded to
\code{\link[flextable:set_table_properties]{flextable::set_table_properties()}}.}
}
\value{
A \code{\link[flextable:flextable]{flextable::flextable()}} object.
}
\description{
Applies the package's house style to a \code{\link[flextable:flextable]{flextable::flextable()}} table, the
Word-facing twin of \code{\link[=theme_gt]{theme_gt()}}: fonts, borders, row striping, caption
alignment, and footnote formatting. Numeric columns (stats, estimates,
p-values) receive reduced sizes, and the \code{digit} font when it is passed.
}
\details{
Sizes are in points, the unit \code{flextable} writes to OOXML, where \code{\link[=theme_gt]{theme_gt()}}
takes pixels; the defaults are tuned for Word rather than converted from the
\code{gt} values. Footnote marks have no counterpart here: they are the ones
\code{\link[gtsummary:as_flex_table]{gtsummary::as_flex_table()}} assigns at conversion.

The caption is set bold, in \code{alpha} at \code{title_font_size}, as direct
formatting: the \verb{Table Caption} style it keeps would otherwise take the
typography of whatever template writes the document. \code{\link[=tbl_caption]{tbl_caption()}} gives
the same typography to a caption set after the theme. A spanner row is ruled
under its labels only, as \code{gt} does, and the column labels sit at the bottom
of their cells.

The fonts default to Aptos rather than to the session font of \code{\link[=set_opts]{set_opts()}}:
a Word table is read on a machine that holds the Office families and rarely
anything else. \code{\link[=easy_out]{easy_out()}} declares Calibri as its fallback.
}
\examples{
set_opts()

ft <- flextable::flextable(head(penguins)) |>
  theme_ft()

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opts.R
\name{set_opts}
\alias{set_opts}
\title{Build the package options object}
\usage{
set_opts(
  .default_font = "sans",
  .vars_envir = NULL,
  .assign = TRUE,
  .name = "opts",
  .reactable = TRUE,
  ...
)
}
\arguments{
\item{.default_font}{Fallback font family, used when a family requested
through \code{font} is unavailable on the system. Passed to \code{\link[=check_fonts]{check_fonts()}} as
its \code{.default}. It does not fill the text (\code{alpha}) and numeric (\code{digit})
font slots itself: those default to the OS-agnostic system sans-serif
(\code{"sans"}) and are set through \code{font}. Defaults to \code{"sans"}.}

\item{.vars_envir}{Optional variable classification (as produced by
\code{\link[=easy_descr]{easy_descr()}}) used to resolve the deferred \code{opts$vars} formulas. When
\code{NULL} (default), the formulas resolve against the \code{.vars_context} cached by
\code{\link[=use_vars]{use_vars()}} at evaluation time.}

\item{.assign}{Logical. If \code{TRUE} (default), the options object is stored under
\code{.name} in the package's internal store; if \code{FALSE}, it is returned.}

\item{.name}{Name under which the options object is stored in, and retrieved
from, the package's internal store. Defaults to \code{"opts"}, the only name the
package reads on its own: the option-backed argument defaults across themes
and tables target it by name. Another name stays reachable through
\code{\link[=get_opts]{get_opts()}} and \code{\link[=check_opts]{check_opts()}}, given a matching \code{.name}, so it serves
inspection rather than an alternative active profile.}

\item{.reactable}{Logical. Whether to set the session option
\code{reactable.theme} to \code{\link[=theme_rt]{theme_rt()}}, which \code{\link[reactable:reactable]{reactable::reactable()}} reads as
its \code{theme} default. \code{TRUE} (default) sets it, overwriting whatever the
session carried, so that the rendering follows the call rather than the
order of two declarations; \code{FALSE} clears it, which is what returns a
session to the unthemed \code{reactable} default. Only ever touched on the
storing path (\code{.assign = TRUE}) and under the default \code{.name}, an
inspection profile having no business theming the session.}

\item{...}{Named overrides for existing option keys, or new named entries
added to the options object. Unknown names are accepted as user-defined
extensions, readable afterwards via \code{\link[=check_opts]{check_opts()}} and \code{\link[=get_opts]{get_opts()}}.}
}
\value{
The options list, invisibly when stored. With \code{.assign = TRUE}
(default) it is also stored under \code{.name} in the package's internal store,
and the session option \code{reactable.theme} is set from \code{.reactable}.
}
\description{
Assembles the centralised options list consumed across the package: statistic
templates, table labels, separators, confidence-interval formatting, acronym
dictionary, page width, fonts, and colours. Labels and formats follow the locale set by
\code{\link[=lang_fr]{lang_fr()}} (comma decimal mark selects French wording) at build time, so
\code{\link[=lang_fr]{lang_fr()}} must run first. The result is either
stored under a name in the package's internal store (the default), from where
\code{\link[=check_opts]{check_opts()}} and \code{\link[=get_opts]{get_opts()}} read it, or returned for inspection. Keys can
be overridden through \code{...}.
}
\details{
Storing the object also sets \code{reactable.theme}, the session option
\code{\link[reactable:reactable]{reactable::reactable()}} reads as its own \code{theme} default, so a widget a
project builds by hand carries the house palette without a line of its own.
See \code{.reactable} for the ways out.
}
\examples{
opts <- set_opts(.assign = FALSE)
opts$sep$ext

}
\seealso{
\code{\link[=use_vars]{use_vars()}}, \code{\link[=check_opts]{check_opts()}}, \code{\link[=lang_fr]{lang_fr()}}

Other configuration:
\code{\link[=check_opts]{check_opts()}},
\code{\link[=clear_vars]{clear_vars()}},
\code{\link[=docx_page_width]{docx_page_width()}},
\code{\link[=get_opts]{get_opts()}},
\code{\link[=lang_fr]{lang_fr()}},
\code{\link[=use_vars]{use_vars()}}
}
\concept{configuration}

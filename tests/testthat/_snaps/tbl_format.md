# gt_format() warns once and delegates to tbl_format()

    Code
      res <- gt_format(tbl)
    Condition
      Warning:
      `gt_format()` was deprecated in hebstr 0.0.0.9000.
      i Please use `tbl_format()` instead.

